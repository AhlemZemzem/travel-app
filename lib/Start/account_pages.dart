import 'package:flutter/material.dart';

class account_pages extends StatelessWidget {
  const account_pages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}