import 'package:flutter/material.dart';

class like_pages extends StatelessWidget {
  const like_pages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}