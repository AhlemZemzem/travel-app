import 'package:flutter/material.dart';

class Localizations_pages extends StatelessWidget {
  const Localizations_pages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}